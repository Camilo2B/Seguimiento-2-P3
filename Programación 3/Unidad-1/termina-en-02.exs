"Hola Mundo"
|>	String.upcase()
|>	String.ends_with?("UNDO")
|>	IO.puts()

# Lo que hace el código es que ambos arrojan true, lo que hace el pipe permite concatenar o encapsular
# Fragmentos de un código del otro archivo que estaba en parentesis haciendo que cada uno funcione como un parentesis


